test.sh		-- generates workload and save raw data results in `results` directory
parse.sh 	-- parses raw data results and generates averages
parse-mix.sh	-- parses averages and generates data for gnuplot 
fig14.gp	-- gnuplot script that generates eps figure
make-all.sh	-- do all of the above
